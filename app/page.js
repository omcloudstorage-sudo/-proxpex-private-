import HomePage from '@/components/marketing/HomePage'

export const metadata = {
  title: 'Proxpex — A roadmap your clients can actually see',
  description:
    'Proxpex turns project status into something visible, not something requested. Roadmaps, sprint boards, permanent records, sign-offs, and invoices your clients can actually see.',
}

export default function Page() {
  return <HomePage />
}
